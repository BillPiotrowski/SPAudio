//
//  Observation.swift
//  WPAudio
//
//  Created by William Piotrowski on 7/1/20.
//  Copyright © 2020 William Piotrowski. All rights reserved.
//
/*
import SPCommon
import Foundation


public enum Observation2 {
    case audioSequencer(sequencerState: AudioSequencer.State, deck: AudioSequencer)
//    case audioTransport(transportState: AudioTransportState)
//    case audioPlayer(playerState: AudioPlayer.State)
    /*
    case playbackEngine(activeDeckState: PlaybackBrain.State)
    case playbackEngineCurrent(playbackItem: PlaybackItem?)
    case playbackEngineQueued(playableItem: PlayableItem?)
    //case oldPlaylist(playlistState: PlaylistHandler.State)
    case cues(cuesState: Cues.State)
    case cartridge(cartridgeState: SequenceLoader.State)
    //case albumsLoader(state: AlbumsLoader.State)
    case playlist(playlist: Album)
    case userSecureData(secureData: UserSecureData)
    case meter(reading: MeterReading)
    case inputEngine(input: InputEngine.InputOption?, isRunning: Bool)
    //case playbackTransition(progress: PlayerCrossfadeProgress)
    case playbackCrossfade(transition: PlaybackCrossfade?)
    
    case cueObserver(state: PurchaseableState)

    
    //case storageListLookup(storageListLookupState: StorageListLookup.State)
 */
    case audioEngine
}
*/
